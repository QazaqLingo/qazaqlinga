const mongoose = require('mongoose'); const { Schema } = mongoose; const schema = new Schema({ userId:{type:Schema.Types.ObjectId,ref:'User',required:true}, questName:String, questType:String, target:Number, current:{type:Number,default:0}, xpReward:{type:Number,default:0}, completed:{type:Boolean,default:false} }, {timestamps:true}); module.exports = mongoose.model('UserQuest', schema);
