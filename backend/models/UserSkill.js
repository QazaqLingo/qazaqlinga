const mongoose = require('mongoose'); const { Schema } = mongoose; const schema = new Schema({ userId:{type:Schema.Types.ObjectId,ref:'User',required:true}, skillName:{type:String,required:true}, progress:{type:Number,default:0}, level:{type:Number,default:1} }, {timestamps:true}); schema.index({userId:1,skillName:1},{unique:true}); module.exports = mongoose.model('UserSkill', schema);
